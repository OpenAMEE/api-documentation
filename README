This is the documentation for AMEEconnect, written in docbook 4.4.

To build, go into the src directory and run: ../tools/book-dist.py 

options:
  --html: HTML single page
  --html-chunk: HTML multi page
  --pdf: PDF
  
  
Requirements:
  xsltproc
  fop (PDF only)